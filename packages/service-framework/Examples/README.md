# Examples

These examples show the same core Service Framework running in different host environments:

- `plain-web`
- `react`
- `three`
- `react-three`

Each example consumes the centralized TypeScript framework core and only adds host-specific bindings where needed.
