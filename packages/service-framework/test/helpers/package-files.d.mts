/** Read a file, by path relative to the package root. */
export declare function readPackageText(path: string): string;
/** List a directory's entries, by path relative to the package root. */
export declare function listPackageDir(path: string): string[];
